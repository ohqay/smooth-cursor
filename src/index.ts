// Obsidian loads this default export at runtime, even though our local code does not import it.
// eslint-disable-next-line import/no-unused-modules
import SmoothCursorPlugin from "./plugin/SmoothCursorPlugin";

export default SmoothCursorPlugin;